---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: kube-ruitenbeek-dev-certificate
  namespace: default
spec:
  secretName: wild-kube-cert
  issuerRef:
    name: acme-issuer
    kind: ClusterIssuer
  dnsNames:
    - '*.kube.ruitenbeek.dev'